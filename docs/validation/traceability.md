# Requirement traceability matrix

Generated from a live test run (commit 324e677, node v23.11.0, 2026-07-30T21:05:34.714Z); regenerate with `pnpm validation:artifacts`.
Join key: the requirement token appearing verbatim in test names, so this
matrix cannot drift from the suite without showing it.

| Requirement | Mechanism | Verifying tests | Result |
| --- | --- | --- | --- |
| DM-P1 — every field auto-derived or authoritative, never both | Source adapters with per-field capability declarations; metrics skip rather than approximate when a source cannot supply a field; milestone/deliverable facts owned here and nowhere else | frame schemas (DM-P1: one normalized shape per fact) accepts a conforming query row<br>frame schemas (DM-P1: one normalized shape per fact) rejects unknown keys — adapters cannot smuggle source-specific fields<br>frame schemas (DM-P1: one normalized shape per fact) rejects non-ISO timestamps<br>edc-core adapter (reference EDC, recorded fixtures) does not pass record creation off as an enrollment date (DM-P1)<br>edc-core adapter (reference EDC, recorded fixtures) declares visits unsupported and refuses to extract them (DM-P1: no silent approximation)<br>github adapter (recorded fixtures, ADR-0012) declares EDC frames unsupported and refuses to extract them (DM-P1: no silent approximation)<br>capability gating (DM-P1: skip, never silently approximate) entry_lag is unavailable when the source cannot supply visits.visit_date<br>capability gating (DM-P1: skip, never silently approximate) query_tat_median runs on the same source, with derived fields annotated<br>capability gating (DM-P1: skip, never silently approximate) milestone_slip is always available — its source is dmops-core itself<br>stat module (ADR-0011) DM-P1: a stat-module study serves the dm set plus the DS starter set (ADR-0012)<br>stat module (ADR-0011) DM-P1: the module boundary hides stat metrics from a dm-only study entirely (ADR-0011)<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: study-grain history spans reporting periods, newest first<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: the site drill-down serves the same versioned metric at site grain<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P5: the site drill-down is row-scoped like every other read<br>metrics surface (DM-P1, DM-P2, DM-P3) a study-grain-only metric returns an empty site list, not an error | PASS |
| DM-P2 — metrics are code, not dashboard configuration | One YAML definition + one tested compute function per metric version; registration copies the YAML verbatim with a checksum; changing a file without a version bump is a hard error | metric dictionary (DM-P2) every YAML definition has a registered compute function of the same version, and vice versa<br>metric dictionary (DM-P2) definitions carry the full written spec, not a label<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: study-grain history spans reporting periods, newest first<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: the site drill-down serves the same versioned metric at site grain<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P5: the site drill-down is row-scoped like every other read<br>metrics surface (DM-P1, DM-P2, DM-P3) a study-grain-only metric returns an empty site list, not an error | PASS |
| DM-P3 — snapshots are immutable and dated | Forbid-mutation triggers on metric_snapshot, source_extract, metric_definition, milestone_rebaseline; DML-only app role additionally lacks UPDATE/DELETE on them; every snapshot references its extract checksum | append-only warehouse and audit trail (DM-P3) rejects UPDATE on audit_event at the database level<br>append-only warehouse and audit trail (DM-P3) rejects DELETE on audit_event<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on metric_snapshot<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on source_extract<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on metric_definition (a changed definition is a new version)<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on milestone_rebaseline (a re-baseline is history, ADR-0009)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot create tables (no DDL)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot INSERT audit_event directly (cannot fabricate audit)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot TRUNCATE domain tables<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot UPDATE or DELETE metric_snapshot even before the trigger fires (DM-P3)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot UPDATE or DELETE milestone_rebaseline even before the trigger fires (ADR-0009)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot disable triggers (requires table ownership)<br>runtime role privilege ceilings (ADR-0003, DM-P3) still audits writes it is allowed to make, attributed via withActor settings<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: study-grain history spans reporting periods, newest first<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: the site drill-down serves the same versioned metric at site grain<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P5: the site drill-down is row-scoped like every other read<br>metrics surface (DM-P1, DM-P2, DM-P3) a study-grain-only metric returns an empty site list, not an error | PASS |
| DM-P4 — displays regulated records, does not hold them | deliverable table stores status + eTMF URI only; uat_cycle mirrors script execution as counts + evidence URI, never the executed package; no signature columns, no file storage anywhere in the schema | display-only posture (DM-P4) has no signature columns anywhere in the schema<br>display-only posture (DM-P4) stores deliverable evidence as an eTMF pointer, not content<br>deliverable surface (ADR-0006) DM-P4: deliverables serve status and an eTMF pointer, never content or signatures<br>UAT cycles and defects (ADR-0010) DM-P4: UAT serves cycle status, counts, and an evidence pointer — never scripts, screenshots, or signatures | PASS |
| DM-P5 — role-scoped views over one set of facts | role × study assignment scoping on every read; sponsor serialization excludes internal fields (blocker notes, re-baseline reasons, defect resolution notes); one underlying view per fact | canWriteAnalysis (ADR-0011) DM-P5: the posture is study-scoped — the same roles on another study cannot<br>role-scoped views over one set of facts (DM-P5) DM-P5: qa sees the whole portfolio; the sponsor seat sees only its study<br>role-scoped views over one set of facts (DM-P5) DM-P5: the sponsor serialization carries no blocker notes; the DM lead's does<br>role-scoped views over one set of facts (DM-P5) DM-P6: the board is one read; planned/forecast/actual arrive as the triple, never collapsed<br>deliverable surface (ADR-0006) DM-P5: deliverable reads are row-scoped; the sponsor seat sees its study only<br>re-baselining governance (ADR-0009, ADR-0003) DM-P5: re-baseline history serves dates to everyone; reasons are omitted from the sponsor serialization<br>UAT cycles and defects (ADR-0010) DM-P5: defect reads are row-scoped; the sponsor serialization omits resolution notes<br>stat module (ADR-0011) DM-P5: the board serves analysis rows only where the module is enabled<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P5: the site drill-down is row-scoped like every other read | PASS |
| DM-P6 — read-heavy, write-light | Board and summary reads are single-view queries; the operational writes are the milestone, deliverable-status, and UAT cycle/defect operations plus the governed re-baseline action, all audited via withActor | canWriteAnalysis (ADR-0011) DM-P6: programmer and biostat assigned to the study can write analysis-phase work<br>role-scoped views over one set of facts (DM-P5) DM-P6: the board is one read; planned/forecast/actual arrive as the triple, never collapsed<br>deliverable surface (ADR-0006) DM-P6: read-only roles cannot write deliverable status; the DM lead's write is audit-attributed (ADR-0003)<br>re-baselining governance (ADR-0009, ADR-0003) DM-P6: the dm_manager's re-baseline moves planned_date, never baseline_date, and both writes are audit-attributed (ADR-0003)<br>UAT cycles and defects (ADR-0010) DM-P6: the analyst's defect write lands and is audit-attributed (ADR-0003)<br>stat module (ADR-0011) DM-P6: the programmer's analysis-phase write lands and is audit-attributed (ADR-0003)<br>stat module (ADR-0011) DM-P6: the biostatistician writes analysis milestones, but DM-phase milestones stay leadership-only<br>stat module (ADR-0011) DM-P6: the analysis posture does not leak sideways — the analyst gets 403 on analysis milestones | PASS |
| DM-Q1 — query_tat_median v1.0 (calendar days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q1: query_tat_median v1.0 (calendar days, history-pinned) matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q2 — query_open_aging v1.0 matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q2: query_open_aging matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q3 — entry_lag v1.0 (calendar days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q3: entry_lag v1.0 (calendar days, history-pinned) matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q4 — milestone_slip v1.0 matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q4: milestone_slip matches hand-computed truth on constructed milestone facts<br>metric qualification against hand-computed fixtures DM-Q4: milestone_slip returns null with zero records when nothing completed in period | PASS |
| DM-Q5 — query_tat_median v1.1 (business days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q5: query_tat_median v1.1 (business days) matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q6 — entry_lag v1.1 (business days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q6: entry_lag v1.1 (business days) matches hand-computed truth for DMOPS-001 | PASS |
| DS-Q1 — pr_review_tat_median v1.0 (business days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | DS metric qualification against hand-computed fixtures (stat module, ADR-0012) DS-Q1: pr_review_tat_median v1.0 (business days) matches hand-computed truth for DMOPS-001 | PASS |
| DS-Q2 — pr_cycle_time_median v1.0 (business days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | DS metric qualification against hand-computed fixtures (stat module, ADR-0012) DS-Q2: pr_cycle_time_median v1.0 (business days) matches hand-computed truth for DMOPS-001 | PASS |
| DS-Q3 — issue_closure_lag_median v1.0 (calendar days) matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | DS metric qualification against hand-computed fixtures (stat module, ADR-0012) DS-Q3: issue_closure_lag_median v1.0 (calendar days) matches hand-computed truth for DMOPS-001 | PASS |
| DS-Q4 — issue_open_aging v1.0 matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | DS metric qualification against hand-computed fixtures (stat module, ADR-0012) DS-Q4: issue_open_aging v1.0 matches hand-computed truth for DMOPS-001 | PASS |

Every mapped requirement has at least one automated verification.
