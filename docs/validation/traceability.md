# Requirement traceability matrix

Generated from a live test run (commit 6e1874c, node v23.11.0, 2026-07-30T16:41:16.301Z); regenerate with `pnpm validation:artifacts`.
Join key: the requirement token appearing verbatim in test names, so this
matrix cannot drift from the suite without showing it.

| Requirement | Mechanism | Verifying tests | Result |
| --- | --- | --- | --- |
| DM-P1 — every field auto-derived or authoritative, never both | Source adapters with per-field capability declarations; metrics skip rather than approximate when a source cannot supply a field; milestone/deliverable facts owned here and nowhere else | frame schemas (DM-P1: one normalized shape per fact) accepts a conforming query row<br>frame schemas (DM-P1: one normalized shape per fact) rejects unknown keys — adapters cannot smuggle source-specific fields<br>frame schemas (DM-P1: one normalized shape per fact) rejects non-ISO timestamps<br>edc-core adapter (reference EDC, recorded fixtures) does not pass record creation off as an enrollment date (DM-P1)<br>edc-core adapter (reference EDC, recorded fixtures) declares visits unsupported and refuses to extract them (DM-P1: no silent approximation)<br>capability gating (DM-P1: skip, never silently approximate) entry_lag is unavailable when the source cannot supply visits.visit_date<br>capability gating (DM-P1: skip, never silently approximate) query_tat_median runs on the same source, with derived fields annotated<br>capability gating (DM-P1: skip, never silently approximate) milestone_slip is always available — its source is dmops-core itself<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage | PASS |
| DM-P2 — metrics are code, not dashboard configuration | One YAML definition + one tested compute function per metric version; registration copies the YAML verbatim with a checksum; changing a file without a version bump is a hard error | metric dictionary (DM-P2) every YAML definition has a registered compute function of the same version, and vice versa<br>metric dictionary (DM-P2) definitions carry the full written spec, not a label<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage | PASS |
| DM-P3 — snapshots are immutable and dated | Forbid-mutation triggers on metric_snapshot, source_extract, metric_definition; DML-only app role additionally lacks UPDATE/DELETE on them; every snapshot references its extract checksum | append-only warehouse and audit trail (DM-P3) rejects UPDATE on audit_event at the database level<br>append-only warehouse and audit trail (DM-P3) rejects DELETE on audit_event<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on metric_snapshot<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on source_extract<br>append-only warehouse and audit trail (DM-P3) rejects UPDATE and DELETE on metric_definition (a changed definition is a new version)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot create tables (no DDL)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot INSERT audit_event directly (cannot fabricate audit)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot TRUNCATE domain tables<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot UPDATE or DELETE metric_snapshot even before the trigger fires (DM-P3)<br>runtime role privilege ceilings (ADR-0003, DM-P3) cannot disable triggers (requires table ownership)<br>runtime role privilege ceilings (ADR-0003, DM-P3) still audits writes it is allowed to make, attributed via withActor settings<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P2: every dictionary metric appears with its version and availability<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P1: a study without a source reports adapter metrics unavailable, not zero<br>metrics surface (DM-P1, DM-P2, DM-P3) DM-P3: snapshot history is served from immutable rows with extract lineage | PASS |
| DM-P4 — displays regulated records, does not hold them | deliverable table stores status + eTMF URI only; no signature columns, no file storage anywhere in the schema | display-only posture (DM-P4) has no signature columns anywhere in the schema<br>display-only posture (DM-P4) stores deliverable evidence as an eTMF pointer, not content | PASS |
| DM-P5 — role-scoped views over one set of facts | role × study assignment scoping on every read; sponsor serialization excludes internal fields (blocker notes); one underlying view per fact | role-scoped views over one set of facts (DM-P5) DM-P5: qa sees the whole portfolio; the sponsor seat sees only its study<br>role-scoped views over one set of facts (DM-P5) DM-P5: the sponsor serialization carries no blocker notes; the DM lead's does<br>role-scoped views over one set of facts (DM-P5) DM-P6: the board is one read; planned/forecast/actual arrive as the triple, never collapsed | PASS |
| DM-P6 — read-heavy, write-light | Board and summary reads are single-view queries; the only slice-1 write is the milestone PATCH, audited via withActor | role-scoped views over one set of facts (DM-P5) DM-P6: the board is one read; planned/forecast/actual arrive as the triple, never collapsed | PASS |
| DM-Q1 — query_tat_median matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q1: query_tat_median matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q2 — query_open_aging matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q2: query_open_aging matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q3 — entry_lag matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q3: entry_lag matches hand-computed truth for DMOPS-001 | PASS |
| DM-Q4 — milestone_slip matches hand-computed expected values | Pure compute function tested against fixtures/study-DMOPS-001/expected-values.json | metric qualification against hand-computed fixtures DM-Q4: milestone_slip matches hand-computed truth on constructed milestone facts<br>metric qualification against hand-computed fixtures DM-Q4: milestone_slip returns null with zero records when nothing completed in period | PASS |

Every mapped requirement has at least one automated verification.
