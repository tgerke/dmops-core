{
  "name": "dmops-core",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:22",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "hostRequirements": {
    "cpus": 4
  },
  "containerEnv": {
    "COREPACK_ENABLE_DOWNLOAD_PROMPT": "0"
  },
  "forwardPorts": [5175, 8788],
  "portsAttributes": {
    "5175": { "label": "dmops-core web", "onAutoForward": "openBrowser" },
    "8788": { "label": "dmops-core api", "onAutoForward": "silent" }
  },
  "postCreateCommand": "corepack enable && pnpm install && cp .env.example .env && docker compose up -d --wait && pnpm db:migrate && pnpm db:seed",
  "postStartCommand": "docker compose up -d && nohup pnpm dev > /tmp/dmops-dev.log 2>&1 &"
}
